package main.model.boardSystem.tiles;

import javafx.scene.paint.Color;

public class GrassTile extends Tile {

    public GrassTile() {
        Color tileColor = new Color(0, 0.5, 0, 1.0);
        this.setFill(tileColor);
    }
}
